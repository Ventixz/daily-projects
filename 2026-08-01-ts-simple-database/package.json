{
  "name": "minidb",
  "version": "1.0.0",
  "description": "A tiny persistent, paged database engine and REPL, TypeScript port of cstack's db_tutorial (C).",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/**/*.test.js",
    "start": "npm run build && node dist/main.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
