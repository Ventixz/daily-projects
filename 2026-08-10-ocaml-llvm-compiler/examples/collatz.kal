extern print_int(x);

def collatz_len(n, count) =
  if n == 1 then count
  else if n - (n / 2) * 2 == 0 then collatz_len(n / 2, count + 1)
  else collatz_len(n * 3 + 1, count + 1);

def main() =
  for n = 1, 20 in
    print_int(collatz_len(n, 0));
